<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <RelativeLayout
        android:id="@+id/titile"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:padding="10dp"
        android:background="@color/blue_FF4DF0FF"
        >
        <CheckBox
            android:id="@+id/main_ckeck_box"
            android:layout_width="35dp"
            android:layout_height="match_parent"
            android:visibility="gone"
            android:layout_alignParentLeft="true"/>
        <TextView
            android:id="@+id/tv_app_name"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:text="@string/zh_CN_app_name"
            android:gravity="center"
            android:textColor="@color/black"
            android:layout_centerInParent="true"
            />

        <ImageView
            android:id="@+id/main_search"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@mipmap/search"
            android:layout_margin="1dp"
            android:layout_alignParentRight="true"/>
        <ImageView
            android:id="@+id/main_del"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@mipmap/ashbin"
            android:visibility="gone"
            android:layout_marginRight="10dp"
            android:layout_toLeftOf="@+id/main_search"

            />
    </RelativeLayout>
    <ListView
        android:id="@+id/main_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/titile">
    </ListView>

    <ImageView
        android:id="@+id/main_add"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="@drawable/main_add_iv"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_margin="30dp"
        android:src="@mipmap/add_circle"
        />
</RelativeLayout>